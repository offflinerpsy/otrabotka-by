# Правила и документация проекта figma_export

> Корпоративный сайт ООО «Трейднефтепром» - компания по переработке отработанных масел
> Домен: otrabotka.by

---

## 📁 Структура проекта

```
figma_export/
├── src/
│   ├── App.tsx              # 1714 строк - МОНОЛИТНЫЙ главный файл
│   ├── main.tsx             # Точка входа React
│   ├── index.css            # Tailwind CSS (3218 строк)
│   ├── components/
│   │   ├── Header.tsx       # 190 строк - Навигация
│   │   ├── Footer.tsx       # 175 строк - Подвал (ИСПОЛЬЗУЕТСЯ)
│   │   ├── CompanyCard.tsx  # 384 строки - Модальное окно реквизитов
│   │   ├── AboutPage.tsx    # 12 строк - Обёртка для OilProcessingSection
│   │   ├── OilProcessingSection.tsx  # ~410 строк - Страница "О нас"
│   │   ├── figma/
│   │   │   └── ImageWithFallback.tsx  # Компонент изображения с fallback
│   │   └── ui/              # 48+ Radix UI компонентов
│   ├── assets/              # PNG из Figma + JPG hero-изображения
│   ├── imports/             # SVG path data
│   └── styles/
│       └── globals.css      # CSS переменные и токены
├── public/images/           # Публичные изображения
├── vite.config.ts           # 68 строк - Алиасы и конфигурация
└── package.json
```

---

## 🛠 Технологический стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| React | 18.3.1 | UI фреймворк |
| Vite | 6.3.5 | Сборщик с SWC |
| TypeScript | implicit | Типизация через Vite |
| Tailwind CSS | 4.1.3 | Стилизация |
| motion/react | 12+ | Анимации (Framer Motion) |
| Radix UI | 48+ компонентов | Доступные UI примитивы |
| Lucide React | 0.487.0 | Иконки |

---

## 📊 Ключевые структуры данных

### SITE_CONFIG (App.tsx, строки 35-45)
```typescript
const SITE_CONFIG = {
  phone: "+375292007007",
  phoneDisplay: "+375 29 200-70-07",
  email: "admin@otrabotka.by",
  address: "Беларусь, г. Минск, ул. Казинца, 4А",
  companyName: "Трейднефтепром",
  companyFullName: "ООО «Трейднефтепром»",
  workingHours: "Пн-Вс: 09:00-18:00"
};
```

### PAGES_STRUCTURE (App.tsx, строки 46-202)
```typescript
// 13 страниц с группами:
// - main (главная)
// - services (4 услуги)
// - points (9 пунктов приёма)
// - about
// - contacts
```

### companyData (CompanyCard.tsx)
```typescript
// Полные реквизиты компании:
// - Юридическое название
// - Директор: Шацкий Д.И.
// - УНП: 193612381
// - ОКПО: 502935217000
// - Банковские реквизиты (ОАО «БНБ-Банк»)
// - Адреса (юридический/фактический)
```

---

## 🧭 Система навигации

**Тип**: State-based (без react-router)

```typescript
// App.tsx
const [currentRoute, setCurrentRoute] = useState<string>('home');

// Навигация через CustomEvent
window.dispatchEvent(new CustomEvent('navigate', { detail: { route } }));

// Рендеринг через switch
const renderPage = () => {
  switch (currentRoute) {
    case 'home': return <HomePage />;
    case 'about': return <AboutPage />;
    // ...
  }
};
```

**Маршруты**:
- `home` - Главная страница
- `about` - О компании (OilProcessingSection)
- `contacts` - Контакты
- `services-*` - Услуги (4 страницы)
- `points-*` - Пункты приёма (9 страниц)

---

## 🎨 Дизайн-система

### Цветовая схема
```css
--primary: #fcd900;        /* Жёлтый - основной */
--primary-dark: #e5c400;   /* Жёлтый тёмный */
--background: #0a0a0a;     /* Почти чёрный */
--text: #ffffff;           /* Белый */
--text-muted: #a1a1aa;     /* Серый */
```

### Паттерны стилизации
- **Glassmorphism**: `bg-black/50 backdrop-blur-md border border-white/10`
- **Градиенты**: `bg-gradient-to-b from-black/80 via-black/40 to-transparent`
- **Тени**: `shadow-lg shadow-primary/20`
- **Закругления**: `rounded-xl`, `rounded-2xl`

### Анимации (motion/react)
```typescript
// Параллакс на hero-секциях
const { scrollYProgress } = useScroll();
const y = useTransform(scrollYProgress, [0, 1], [0, -100]);

// Entry анимации
initial={{ opacity: 0, y: 20 }}
animate={{ opacity: 1, y: 0 }}
transition={{ duration: 0.6 }}
```

---

## 📦 Алиасы Vite (vite.config.ts)

### Путь к src
```typescript
'@': path.resolve(__dirname, './src')
```

### Figma ассеты
```typescript
'figma:asset/0ef91467...png': './src/assets/0ef91467...png'
// + ещё 7 PNG файлов
```

### Пакеты (версионированные)
```typescript
'lucide-react': 'lucide-react@0.487.0'
'@radix-ui/react-*': '@radix-ui/react-*@версия'
// Все Radix UI компоненты с фиксированными версиями
```

---

## ⚠️ Известные проблемы

### 1. TypeScript ошибки импортов (КРИТИЧНО)
**Файл**: `OilProcessingSection.tsx`, строки 2-3
```typescript
// НЕПРАВИЛЬНО:
import heroImage from '@/assets/oil-refinery-hero.jpg';

// ПРАВИЛЬНО:
import heroImage from '../assets/oil-refinery-hero.jpg';
```

### 2. Мёртвый код
**Файл**: `App.tsx`, строки 1587-1678
- Содержит неиспользуемую функцию `Footer`
- Реальный Footer импортируется из `components/Footer.tsx`
- **Решение**: Удалить функцию Footer из App.tsx

### 3. Дублирование SITE_CONFIG
**Файлы**: App.tsx, Header.tsx, Footer.tsx
- Одинаковые данные в 3 файлах
- **Решение**: Вынести в `src/config/site.ts`

### 4. Неправильные координаты карты
**Файл**: `App.tsx`, строка ~1414
- Yandex карта показывает Дагестан вместо Минска
- **Решение**: Исправить координаты на 53.8515,27.4770

---

## 📝 Правила модификации

### Добавление новой страницы
1. Добавить в `PAGES_STRUCTURE` (App.tsx, строки 46-202)
2. Создать компонент в `src/components/`
3. Добавить case в `renderPage()` (App.tsx)
4. Добавить пункт в навигацию Header.tsx

### Изменение контактных данных
1. Обновить `SITE_CONFIG` в **App.tsx**
2. Обновить `SITE_CONFIG` в **Header.tsx**
3. Обновить `SITE_CONFIG` в **Footer.tsx**
4. Обновить `companyData` в **CompanyCard.tsx** (для реквизитов)

### Добавление изображений
1. Поместить в `src/assets/` или `public/images/`
2. Для src/assets - использовать import: `import img from '../assets/file.jpg'`
3. Для public - использовать путь: `/images/file.jpg`

### Стилизация компонентов
1. Использовать Tailwind классы
2. Для glassmorphism: `bg-black/50 backdrop-blur-md border border-white/10`
3. Для hover-эффектов: `hover:border-primary/30 transition-all duration-300`
4. Анимации через motion/react, не CSS animations

---

## 🔧 Команды разработки

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера (порт 3000)
npm run dev

# Сборка продакшена
npm run build

# Превью сборки
npm run preview
```

---

## 📋 Чеклист перед коммитом

- [ ] Нет TypeScript ошибок (`npm run build`)
- [ ] SITE_CONFIG синхронизирован во всех файлах
- [ ] Новые страницы добавлены в PAGES_STRUCTURE
- [ ] Изображения оптимизированы
- [ ] Протестировано на мобильных устройствах
- [ ] Проверены все ссылки навигации

---

## 🏗 Компоненты в деталях

### App.tsx - Главный файл

| Строки | Содержимое |
|--------|------------|
| 1-34 | Импорты |
| 35-45 | SITE_CONFIG |
| 46-202 | PAGES_STRUCTURE (13 страниц) |
| 203-250 | PhoneIcon, ArrowIcon |
| 251-350 | Breadcrumbs компонент |
| 351-550 | HeroWithParallax компонент |
| 551-850 | HomePage компонент |
| 851-1000 | VacuumGallery компонент |
| 1001-1200 | ServicesPage компонент |
| 1201-1400 | PointsPage компонент |
| 1401-1586 | ContactsPage компонент |
| 1587-1678 | Footer (МЁРТВЫЙ КОД) |
| 1679-1714 | Главный рендер App |

### OilProcessingSection.tsx - Страница "О нас"

| Строки | Содержимое |
|--------|------------|
| 1-20 | Импорты (ОШИБКА в строках 2-3) |
| 21-60 | processSteps[] - 5 этапов переработки |
| 61-100 | advantages[] - преимущества вакуумной дистилляции |
| 101-140 | applications[] - области применения |
| 141-180 | distillateResults[] - результаты дистилляции (%) |
| 181-410 | JSX разметка с анимациями |

---

*Последнее обновление: 5 января 2026*
